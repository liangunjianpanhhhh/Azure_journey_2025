第二单元 什么是微软 Azure
提供的服务：
1.通过在可信赖的平台上构建，利用行业领先的 AI 和云服务推动您的组织发展。
2.跨集成平台高效管理所有基础结构、数据、分析和 AI 解决方案。
3.依靠一个致力于安全和负责的合作伙伴提供的值得信赖的技术。
探索：
将其现有应用程序迁移到在 Azure 中运行的虚拟机（VM）。 将现有应用迁移到 VM 是一个很好的开端，但云远不止于运行 VM 的不同位置
第三单元 开始使用 Azure 帐户
一个 Azure 订阅
订阅在“learn 沙盒”中运行，有账户后可以订阅多个，在每个订阅中创建资源组，每个资源组对应多个 Azure 资源
创建 Azure 账户（购买）
microsoft learn 沙盒：创建临时订阅，支持免费创建和测试 Azure 资源，会自动清理临时资源
第四单元 练习 - 探索 Learn 沙盒-没用自带的 用的vs code
1.Get-date 使用 PowerShell Get-date 命令获取当前日期和时间
2025年3月5日 18:50:46
2.az version 使用 Azure 命令来检查现使用的 CLI 版本
3.输入 bash 以切换到 BASH CLI
4.使用 date 命令获取当前日期和时间
(Azure CLI):date
可以使用字母 az 在 BASH 模式下启动 Azure 命令
5.输入 az interactive 进入交互模式
6.exit 命令退出交互模式
第五单元 描述 Azure 物理基础结构
Azure 的核心体系结构组件可以分为两大类：物理基础结构和管理基础结构
Azure 的物理基础结构从数据中心开始
它们是将资源安排在机架中的设施，具有专用的电源、冷却和网络基础结构
单独的数据中心无法直接访问，数据中心分为 Azure 区域或 Azure 可用性区域
区域是指地球上的某个地理区域，包含至少一个（但很可能是多个）数据中心，这些数据中心彼此相邻并通过一个低延迟的网络相互连接
（智能分配）
还有一些全球性 Azure 服务不需要用户选择特定的区域，例如 Microsoft Entra、Azure 流量管理器和 Azure DNS
每个可用性区域都由一个或多个数据中心组成
Azure 可以通过可用性区域帮助实现应用的高可用性（设置冗余数据）
在可用性区域之间复制服务和传输数据可能会产生费用
支持可用性区域的 Azure 服务：
区域性服务：将资源（例如 VM、托管磁盘和 IP 地址）固定到特定的区域。
区域冗余服务：平台自动跨区域复制（例如区域冗余存储和 SQL 数据库）。
非地理区域服务：服务始终从 Azure 地理位置提供，可以灵活应对局部区域和地理区域范围的服务中断
大多数 Azure 区域都会与至少距其 300 英里外的另一区域配对（适用于跨地域复制资源，减少服务中断的可能性）
优点
1.如果发生大规模 Azure 服务中断，则优先考虑每个区域对中的一个区域，确保针对该区域对中托管的应用程序尽快恢复至少一个区域
2.更新
3.税务法律
在单向配对中，主要区域不会为其次要区域提供备份
主权区域是与 Azure 主实例隔离的 Azure 实例
第六单元 描述 Azure 管理基础结构
资源是 Azure 的基本构建基块
资源组对资源进行分组，可以包含多个资源，但一个资源一次只能位于一个资源组中
某个资源组应用操作时，该操作将应用于该资源组中的所有资源
订阅是 Azure 中管理、计费和缩放的一个单位
一个帐户可以有多个订阅，但只需要一个订阅
可以选择创建额外的订阅来区分：环境、组织结构、计费
资源会被收集到资源组中，而资源组会被收集到订阅中，Azure 管理组提供高于订阅的范围级别
管理组将订阅整理到名为“管理组”的容器中，并将治理条件应用到管理组
管理组中的所有订阅都将自动继承应用于管理组的条件
一个管理组树最多可支持六个级别的深度，每个管理组和订阅只能支持一个父项
第七单元 练习，在vs code上进行
