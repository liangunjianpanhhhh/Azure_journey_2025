第二单元
描述 azure 虚拟机
借助 VM 可以在云中创建和使用 VM
VM 以虚拟服务器的形式提供基础结构即服务 (IaaS) 并以许多不同的方式使用
VM 可以做到：操作系统的完全控制+运行自定义软件+自定义托管配置
可以创建或使用已创建的映像快速预配 VM
在 azure 中缩放 VM
单独使用 VM 或者把 VM 组合（可分组）在一起使用
虚拟机规模集
使用虚拟机规模集可以创建并管理一组完全相同的、负载均衡的 VM
相同用途的 VM ：配置全部相同-设置网络路由参数、监视利用率
使用虚拟机规模集，Azure 会自动执行大部分工作
使用虚拟机规模集为计算、大数据和容器工作负荷等领域构建大规模的服务
虚拟机可用性集
可帮助构建更具弹性、高度可用的环境，旨在确保 VM 错开更新并采用不同的电源和网络连接，从而防止因单个网络或电源故障丢失所有 VM
可用性集通过更新域和容错域两种方式对 VM 进行分组
使用 VM 的示例
1.在测试和开发过程中
2.在云中运行应用程序时，在公有云中运行部分应用程序可以带来巨大的经济优势
3.将数据中心扩展到云时，在 Azure 中创建虚拟网络并将 VM 添加到该虚拟网络来扩展其本地网络的功能
4.灾难恢复期间
可以创建物理服务器的映像并将其托管在 VM 中，几乎不需要进行任何更改
第三单元 练习 - 创建 Azure 虚拟机
vs code上进行
第四单元 描述 Azure 虚拟桌面
另一种类型的虚拟机，能够从任何位置使用云托管的 Windows 版本
可以启用多重身份验证来保护用户登录
数据和应用与本地硬件分离
Windows 10 或 Windows 11 企业版多会话操作系统是唯一可在单个 VM 上启用多个并发用户的基于 Windows 客户端的操作系统
第五单元 介绍 Azure 容器
想要在单个主机上运行应用程序的多个实例，则容器是绝佳的选择 
什么是容器？
类似于在单个物理主机上运行多个虚拟机，你可以在单个物理或虚拟主机上运行多个容器，不用管理容器的操作系统
容器旨在让你按需响应更改。 使用容器，可以在出现崩溃或硬件中断时快速重启
Docker
将虚拟机与容器进行比较
容器约等于很多个虚拟机
Azure 容器实例
既无需管理任何虚拟机，也不必采用任何其他服务
Azure 容器实例是一种平台即服务 (PaaS) 选项
Azure Container Apps
在设计中更具弹性
能够立即启动并运行，它们删除了容器管理部分
Azure Kubernetes 服务
一项容器业务流程服务
容器通常用于通过使用微服务体系结构来创建解决方案
第六单元 描述 Azure Functions
一个事件驱动的无服务器计算选项，不需要维护虚拟机或容器，事件在没有任何事件时会唤醒函数，减少继续预配保留的需求
Azure 中的无服务器计算
无服务器计算会对运行的服务器进行抽象
采用无服务器计算，只需为其代码运行的时间付费
Azure Functions 的优点
只关心运行服务的代码，而不关心基础平台或基础结构，需要速度很快
如果函数是无状态的（默认情况下），其行为与每次响应事件时都重启的效果一样。 如果函数是有状态的（称为 Durable Functions），会通过函数传递一个上下文来跟踪先前的活动。
如果开发人员的应用需求发生变化，你可以将项目部署到无服务器环境中
第七单元 描述应用程序托管选项
最初可能会求助于虚拟机 (VM) 或容器
Azure 应用服务
使用应用服务可以采用所选编程语言生成和托管 Web 应用、后台作业、移动后端以及 RESTful API，而无需管理基础结构
任何 Git 存储库进行自动化部署，以支持持续部署模型
Azure 应用服务使你专注于生成和维护应用，而 Azure 侧重于保持环境正常运行
应用服务的类型
可托管最常见的应用服务样式，Web 应用， API 应用， WebJobs，移动应用
优点：
将部署和管理集成到平台内。
可以保护终结点。
可以快速扩展站点以便处理高流量负载。
内置的负载均衡和流量管理器可提供高可用性
web应用
托管 Web 应用的全面支持
API 应用
使用自己选择的语言和框架生成基于 REST 的 Web API
WebJobs
使用 Web 作业功能，在与 Web 应用、API 应用或移动设备相同的上下文中运行程序（.exe、Java、PHP、Python 或 Node.js）或脚本（.cmd、.bat、PowerShell 或 Bash）
通常用于作为应用程序逻辑的一部分运行后台任务
移动应用
只需在 Azure 门户中执行一些操作，就可以：
将移动应用数据存储在基于云的 SQL 数据库中。
针对常见的社交媒体提供商对客户进行身份验证，如 MSA、Google、X 和 Facebook。
发送推送通知。
在 C# 或 Node.js 中执行自定义后端逻辑。
